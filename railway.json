{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "dockerfile"
  },
  "deploy": {
    "startCommand": "cd backend && source venv/bin/activate && gunicorn --worker-class eventlet -w 1 app:app",
    "healthcheckPath": "/api/offers",
    "healthcheckTimeout": 30
  }
}
