providers = ["python", "node"]

[phases.setup]
nixPkgs = ["python3", "nodejs", "npm"]

[phases.install]
cmds = [
  "cd frontend && npm ci",
  "cd backend && pip install -r requirements.txt"
]

[phases.build]
cmds = [
  "echo '👨‍👩‍👧‍👦 V19 MULTI-ACCESS BUILD...'",
  "cd frontend && rm -rf dist node_modules/.cache",
  "cd frontend && npm run build"
]

[start]
# Mode Synchrone (4 workers, timeout 120s)
cmd = "cd backend && gunicorn -w 4 -b 0.0.0.0:$PORT --timeout 120 app:app"
