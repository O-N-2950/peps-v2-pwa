providers = ["python", "node"]

[phases.setup]
nixPkgs = ["python3", "nodejs", "npm", "gcc", "libpq-dev"]

[phases.install]
cmds = [
  "cd frontend && npm ci",
  "cd backend && pip install -r requirements.txt"
]

[phases.build]
cmds = [
  "echo '🚀 BUILDING V10.3 STABLE...'",
  "cd frontend && rm -rf dist",
  "cd frontend && npm run build"
]

[start]
# Timeout augmenté à 120s pour éviter les kills prématurés
cmd = "cd backend && gunicorn -k eventlet -w 1 --timeout 120 --log-level debug --bind 0.0.0.0:$PORT app:app"
